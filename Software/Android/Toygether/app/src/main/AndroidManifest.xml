<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ch.epfl.chic.napac.toygether.toygether">

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SplashActivity"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".SignInActivity" />
        <activity android:name=".PasswordForgottenActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".ToysEmptyActivity" />
        <activity android:name=".QRCodeActivity" />
        <activity android:name=".barcode.BarcodeCaptureActivity" />
        <activity android:name=".SaveNewToyActivity" />
        <activity android:name=".ToysOneLinkedActivity" />
        <activity android:name=".SettingsNotificationsActivity"></activity>
    </application>

</manifest>